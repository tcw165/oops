(defun test()
  ""
  (interactive)
  (skip-syntax-backward "_w")
  ;;(set-mark (point))
  (set-mark (point))
  (skip-syntax-forward "_w")
  )

(mark-marker)
(length mark-ring)
(list mark-ring)

(defun reggggggg ()
  ""
  (interactive)
  (message "%s" (use-active-p))
  )
(global-set-key (kbd "C-,") 'reggggggg)
