;; (defvar oops-definition-window-enabled nil
;;   "")

(defun oops-toggle-definition-window ()
  ""
  ;; TODO/FIXME:
  ;; * Show/hide definition window.
  (interactive)
  (when oops-mode
    ;; DO something.
    )
  )

(provide 'oops-definition-core)
